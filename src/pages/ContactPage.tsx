// import { Contact } from "../components/Contact(OLD).tsx"; // Correctly linking Contact.tsx
import { Contact } from "../components/Contact.tsx";

const ContactPage = () => {
  return <Contact />;
};

export default ContactPage;

